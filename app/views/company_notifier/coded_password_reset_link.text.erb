We received a request to reset your EnterShip credentials. To do so, please follow the coded link below:

<%= password_reset_form_url(@company.password_reset_code) %>

If you received this email in error, simply do nothing. The above link will expire at <%= @company.password_reset_expires_at.to_formatted_s(:mail_timestamp) %>.

Thanks!
EnterShip
